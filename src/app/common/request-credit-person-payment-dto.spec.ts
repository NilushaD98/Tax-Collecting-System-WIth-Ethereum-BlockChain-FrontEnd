import { RequestCreditPersonPaymentDTO } from './request-credit-person-payment-dto';

describe('RequestCreditPersonPaymentDTO', () => {
  it('should create an instance', () => {
    expect(new RequestCreditPersonPaymentDTO()).toBeTruthy();
  });
});
