import { PersonPaymentDetail } from './person-payment-detail';

describe('PersonPaymentDetail', () => {
  it('should create an instance', () => {
    expect(new PersonPaymentDetail()).toBeTruthy();
  });
});
